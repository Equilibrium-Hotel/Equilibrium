import React from 'react'

export default class Logout extends React.Component {
  render() {
    return (
      <div>
        <span>Add Logout functionality in this component</span>
      </div>
    );
  }
}